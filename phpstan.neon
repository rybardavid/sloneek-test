includes:
    - vendor/nunomaduro/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 9
    paths:
        - app
        - config
        - database
        - routes
        - tests

    excludePaths:
        - vendor/*
        - storage/*